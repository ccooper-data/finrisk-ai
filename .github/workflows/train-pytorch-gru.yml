name: Train PyTorch GRU
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  train:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: "3.12", cache: pip}
      - run: pip install -e ".[dev,ml]"
      - run: pytest -q
      - uses: actions/download-artifact@v4
        with:
          name: finrisk-sec-cohort-full-36201584816
          path: artifacts/source
          github-token: ${{ github.token }}
          run-id: 36201584816
      - run: finrisk train-pytorch-gru --cohort artifacts/source/sec-cohort-full/sec_labeled_cohort.parquet --out-dir artifacts/modeling/pytorch-gru
      - uses: actions/upload-artifact@v4
        with:
          name: finrisk-pytorch-gru-${{ github.run_id }}
          path: artifacts/modeling/pytorch-gru/
          if-no-files-found: error
          retention-days: 30
