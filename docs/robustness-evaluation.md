# Temporal and subgroup robustness

Headline future-test performance is necessary but insufficient.

Promoted models should be evaluated by calendar year and, where reliable labels exist, meaningful business subgroups such as sector/industry. Small groups are reported as insufficient support rather than assigned unstable AUCs.

This analysis is diagnostic. It must not be used to repeatedly tune against the future test set.
