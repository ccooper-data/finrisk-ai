# Temporal and subgroup robustness

Aggregate future-test performance is necessary but insufficient.

Promoted models should report performance by calendar year and, when reliable labels exist, meaningful business subgroups. Groups with inadequate rows or positives are explicitly marked insufficient support rather than assigned unstable metrics.

Robustness analysis is diagnostic. Future-test slices must not become a hidden hyperparameter-tuning loop.
