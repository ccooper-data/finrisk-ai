# Reproducibility manifest

Evidence-producing runs should record the exact code SHA, frozen artifact identifiers/digests, Python version, relevant package versions and deterministic seed when applicable.

This is separate from the model card: the model card explains what was evaluated; the reproducibility manifest records the software/data environment required to reproduce it.
